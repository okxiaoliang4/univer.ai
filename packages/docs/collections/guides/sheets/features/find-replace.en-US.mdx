import { Callout, Tabs } from 'nextra/components'
import FeatureMeta from '@/components/feature-meta'

# Find & Replace

<FeatureMeta texts={['-', '-', '-', 'UniverSheetsFindReplacePreset' ]} />

The Find and Replace plug-in provides the function of finding and replacing cells.

This feature includes the following plugin packages:

* [`@univerjs/find-replace`](https://www.npmjs.com/package/@univerjs/find-replace/README) - Fundamental Find and Replace Plugin
* [`@univerjs/sheets-find-replace`](https://www.npmjs.com/package/@univerjs/sheets-find-replace/README) - Sheet Find and Replace Plugin

<Callout>
  TODO: add example
</Callout>

## Installation with Presets

<Tabs items={['pnpm', 'npm']}>
  <Tabs.Tab>
    ```shell
    pnpm add @univerjs/find-replace @univerjs/sheets-find-replace
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```shell
    npm install @univerjs/find-replace @univerjs/sheets-find-replace
    ```
  </Tabs.Tab>
</Tabs>

```ts {4-5,13,19}
import { createUniver, defaultTheme, LocaleType, Tools } from '@univerjs/presets';
import { UniverSheetsCorePreset } from '@univerjs/presets/preset-sheets-core';
import sheetsCoreEnUS from '@univerjs/presets/preset-sheets-core/locales/en-US';
import { UniverSheetsFindReplacePreset } from '@univerjs/presets/preset-sheets-find-replace';
import sheetsFindReplaceEnUS from '@univerjs/presets/preset-sheets-find-replace/locales/en-US';

const { univerAPI } = createUniver({
    locale: LocaleType.EN_US,
    locales: {
        enUS: Tools.deepMerge(
            {},
            sheetsCoreEnUS,
            sheetsFindReplaceEnUS 
        ),
    },
    theme: defaultTheme,
    presets: [
        UniverSheetsCorePreset(),
        UniverSheetsFindReplacePreset(),
    ],
});
```

## Advanced Installation
## Advanced Installation

<Tabs items={['pnpm', 'npm']}>
  <Tabs.Tab>
    ```shell
    pnpm add @univerjs/find-replace @univerjs/sheets-find-replace
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```shell
    npm install @univerjs/find-replace @univerjs/sheets-find-replace
    ```
  </Tabs.Tab>
</Tabs>

```typescript {2-5,14-15,20-21}
import { LocaleType, Tools } from '@univerjs/core';
import { UniverFindReplacePlugin } from '@univerjs/find-replace';
import { UniverSheetsFindReplacePlugin } from '@univerjs/sheets-find-replace';
import FindReplaceEnUS from '@univerjs/find-replace/locale/en-US';
import SheetsFindReplaceEnUS from '@univerjs/sheets-find-replace/locale/en-US';

import '@univerjs/find-replace/lib/index.css';

const univer = new Univer({
  theme: defaultTheme,
  locale: LocaleType.EN_US,
  locales: {
    [LocaleType.EN_US]: Tools.deepMerge(
      FindReplaceEnUS,
      SheetsFindReplaceEnUS
    ),
  },
});

univer.registerPlugin(UniverFindReplacePlugin);
univer.registerPlugin(UniverSheetsFindReplacePlugin);
```

import { Tabs } from 'nextra/components'

# Conditional Formatting

| Paid Plan | Univer Server Integration | Univer on Node.js | Included in Preset |
| - | - | - | - |
| - | - | ✅ | `UniverSheetsConditionalFormattingPreset` |

Conditional formatting is a feature that sets rules in cells to automatically set cell formats based on the value of the data.

This feature includes the following plugin packages:

* [`@univerjs/sheets-conditional-formatting`](https://www.npmjs.com/package/@univerjs/sheets-conditional-formatting/README) - fundamental conditional formatting plugin
* [`@univerjs/sheets-conditional-formatting-ui`](https://www.npmjs.com/package/@univerjs/sheets-conditional-formatting-ui/README) - UI plugin for conditional formatting

## Installation with Presets

```ts {4-5,13,19}
import { createUniver, defaultTheme, LocaleType, Tools } from '@univerjs/presets';
import { UniverSheetsCorePreset } from '@univerjs/presets/preset-sheets-core';
import sheetsCoreZhCN from '@univerjs/presets/preset-sheets-core/locales/zh-CN';
import { UniverSheetsConditionalFormattingPreset } from '@univerjs/presets/preset-sheets-conditional-formatting';
import sheetsConditionalFormattingZhCN from '@univerjs/presets/preset-sheets-conditional-formatting/locales/zh-CN';

const { univerAPI } = createUniver({
    locale: LocaleType.ZH_CN,
    locales: {
        zhCN: Tools.deepMerge(
            {},
            sheetsCoreZhCN,
            sheetsConditionalFormattingZhCN 
        ),
    },
    theme: defaultTheme,
    presets: [
        UniverSheetsCorePreset(),
        UniverSheetsConditionalFormattingPreset(),
    ],
});
```

## Advanced Installation

<Tabs items={['pnpm', 'npm']}>
  <Tabs.Tab>
    ```shell
    pnpm add @univerjs/sheets-conditional-formatting @univerjs/sheets-conditional-formatting-ui
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```shell
    npm install @univerjs/sheets-conditional-formatting @univerjs/sheets-conditional-formatting-ui
    ```
  </Tabs.Tab>
</Tabs>

```typescript {2-4,6,8,13,18-19}
import { LocaleType, Tools } from '@univerjs/core';
import { UniverSheetsConditionalFormattingPlugin } from '@univerjs/sheets-conditional-formatting';
import { UniverSheetsConditionalFormattingUIPlugin } from '@univerjs/sheets-conditional-formatting-ui';
import SheetsConditionalFormattingUIZhCN from '@univerjs/sheets-conditional-formatting-ui/locale/zh-CN';

import '@univerjs/sheets-conditional-formatting-ui/lib/index.css';

const univer = new Univer({
  theme: defaultTheme,
  locale: LocaleType.ZH_CN,
  locales: {
    [LocaleType.ZH_CN]: Tools.deepMerge(
      SheetsConditionalFormattingUIZhCN
    ),
  },
});

univer.registerPlugin(UniverSheetsConditionalFormattingPlugin);
univer.registerPlugin(UniverSheetsConditionalFormattingUIPlugin);
```

## Import

```typescript
import '@univerjs/sheets-conditional-formatting-ui/lib/index.css';

import { UniverSheetsConditionalFormattingPlugin } from '@univerjs/sheets-conditional-formatting';
import { UniverSheetsConditionalFormattingUIPlugin } from '@univerjs/sheets-conditional-formatting-ui';
```

## Installation for Univer on Node.js

<Tabs items={['pnpm', 'npm']}>
  <Tabs.Tab>
    ```shell
    pnpm add @univerjs/sheets-conditional-formatting
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```shell
    npm install @univerjs/sheets-conditional-formatting
    ```
  </Tabs.Tab>
</Tabs>

```typescript {2,4,11}
import { LocaleType, Tools } from '@univerjs/core';
import { UniverSheetsConditionalFormattingPlugin } from '@univerjs/sheets-conditional-formatting';

import '@univerjs/sheets-conditional-formatting/facade';

const univer = new Univer({
  theme: defaultTheme,
  locale: LocaleType.ZH_CN,
});

univer.registerPlugin(UniverSheetsConditionalFormattingPlugin);
```

## Links

* [@univerjs/sheets-conditional-formatting API reference](/typedoc/@univerjs/sheets-conditional-formatting/README)
* [@univerjs/sheets-conditional-formatting-ui API reference](/typedoc/@univerjs/sheets-filter-ui/README)
